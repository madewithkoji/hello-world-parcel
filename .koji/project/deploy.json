{
  "deploy": {
    "subdomain": ".withkoji.com",
    "frontend": {
      "output": "dist",
      "commands": [
        "npm install",
        "npm run build"
      ],
      "injections": [
      ]
    }
  }
}
